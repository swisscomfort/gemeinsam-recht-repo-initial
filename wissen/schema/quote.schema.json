{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "gemeinsam-recht/wissen/quote.schema.json",
  "title": "Quote — Zaehlsignal je Vorgehen (Quoten-Fundament, AUFTRAG-W0 Teil D)",
  "description": "Ein Eintrag je Vorgehen und Kontext. Reine Zaehlwerte ab Mindestfallzahl (Invariante 11) — keine Fallinhalte. In W0 gibt es keine echten Daten; die Datei quoten/quoten.json bleibt leer.",
  "type": "object",
  "additionalProperties": false,
  "required": ["vorgehen", "wenn", "n", "positiv", "zeitstand"],
  "properties": {
    "vorgehen": { "type": "string", "minLength": 1 },
    "wenn": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "n": { "type": "integer", "minimum": 0 },
    "positiv": { "type": "integer", "minimum": 0 },
    "zeitstand": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" }
  }
}
