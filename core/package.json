{
  "name": "@gemeinsam-recht/core",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministischer Fristenrechner + Regel-Flags fuer den Startfall Mietkuendigung LU (Plan §4 Schritt 1). Kein LLM, kein Netzwerk, keine Laufzeit-Abhaengigkeiten.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
